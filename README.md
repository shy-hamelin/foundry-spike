# foundry-spike

Throwaway scratch repo for the Grove Foundry feasibility spike: proving a
Managed Agent can make a real multi-file change and open a PR (Test 1 via
GitHub MCP; Test 2 via in-sandbox clone + `make test` + MCP push).

Test command: `make test` (pure stdlib `unittest`, no external deps).
